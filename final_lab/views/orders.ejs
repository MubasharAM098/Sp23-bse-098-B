<h1>Your Orders</h1>
<% if (orders.length > 0) { %>
    <ul>
        <% orders.forEach(order => { %>
            <li>Order ID: <%= order._id %> - Total: $<%= order.totalPrice %></li>
        <% }); %>
    </ul>
<% } else { %>
    <p>No orders yet</p>
<% } %>
